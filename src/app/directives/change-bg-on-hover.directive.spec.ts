import { ChangeBgOnHoverDirective } from './change-bg-on-hover.directive';

describe('ChangeBgOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBgOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
