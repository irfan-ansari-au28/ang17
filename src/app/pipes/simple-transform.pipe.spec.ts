import { SimpleTransformPipe } from './simple-transform.pipe';

describe('SimpleTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
